# Runtime

本目录承载 Service-first Root Runtime Manager 与 Non-root Device Agent 的正式边界。

Root Runtime Manager 维护全局 Service、Set、Endpoint、Link、Device、Topology、Operation 和 Health 状态。Non-root Device Agent 只执行 Root 下发的计划并上报状态。
