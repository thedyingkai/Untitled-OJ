# Native GUI

Root Installer GUI 是 Root Device 上的原生控制面，负责 Service、Set、Endpoint、Link、Topology、Device、Plan、Operation、Health、Log 和 Repository Source 管理。

本轮先建立边界和目录，不以 Web Shell、浏览器、WebView 或 Electron 替代原生 GUI。
