# 基础 Service

基础 Service 包括 Gateway、Web Shell、Problem API、Judge API、Judge Worker、Storage 和 PostgreSQL。

这些 Service 都必须出现在 Runtime、Endpoint、Link 和 Topology 模型中。外部 Storage 或 PostgreSQL 也必须作为 external Service 注册。
