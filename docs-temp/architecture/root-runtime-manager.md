# Root Runtime Manager

Root Runtime Manager 运行在 Root Device 上，负责 Service 安装、删除、启用、禁用、热插拔、Endpoint 配置、Link 配置、Set 展开、Topology 生成、Operation 记录、Health 聚合和 Repository Source 管理。

Gateway 和 Web Shell 都不是系统根。Gateway 是普通 Service，Web Shell 是 Root 侧可热插拔 UI Service。
