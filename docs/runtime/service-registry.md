# Service Registry

Service Registry 记录 Service、Set、安装状态、运行模式、Endpoint、Link、Device、Topology 和 Operation。

正式表使用 service-first 命名，包括 `service_sets`、`service_nodes`、`service_installations`、`service_endpoints`、`service_links`、`service_runtime_operations` 和 `devices`。旧 Module-first 表不再由正式初始化链路创建。
