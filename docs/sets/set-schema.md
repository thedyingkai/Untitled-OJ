# Set Schema

Set 字段包括 `id`、`name`、`description`、`services`、`default_links`、`recommended_for`、`non_root_only` 和 `required_root_links`。

`services` 引用 Service ID。`default_links` 只描述推荐连接，实际 Endpoint 和 Link 由 Root Runtime Manager 配置。
