# Set 模型

Set 是推荐安装集合，不是运行对象。Installer 展示 Set、展开 Service、生成安装计划并展示默认 Link。

当前预设位于 `sets/`：`single-node-oj`、`distributed-root`、`judge-worker-node`、`course-judge`、`contest-basic`、`service-development`。

`contest-basic` 只描述基础服务组合，不表示 Contest 业务已经实现。
