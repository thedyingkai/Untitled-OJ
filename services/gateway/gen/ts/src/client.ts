// Code generated by ojos service. DO NOT EDIT.
export interface Operation {
  readonly id: string;
  readonly method: string;
  readonly path: string;
  readonly audience: string;
  readonly permission?: string;
  readonly headerParameters: readonly HeaderParameter[];
  readonly requestContentTypes: readonly string[];
  readonly requestBodyRequired: boolean;
}

export interface HeaderParameter { readonly name: string; readonly required: boolean; }

export interface ContextSnapshot { readonly generation: number; readonly baseUrl: string; readonly token?: string; }
export type ContextSnapshotProvider = () => ContextSnapshot | Promise<ContextSnapshot>;
export type FetchLike = (input: RequestInfo | URL, init?: RequestInit) => Promise<Response>;

export interface ClientOptions { readonly context: ContextSnapshotProvider; readonly fetch?: FetchLike; readonly timeoutMs?: number; }
export interface CallOptions {
  readonly path?: Readonly<Record<string, string>>;
  readonly query?: Readonly<Record<string, string | number | boolean | undefined>>;
  readonly body?: unknown;
  readonly rawBody?: BodyInit;
  readonly headers?: Readonly<Record<string, string>>;
  readonly contentType?: string;
  readonly signal?: AbortSignal;
  readonly timeoutMs?: number;
  readonly idempotencyKey?: string;
}

export type ClientErrorKind = 'configuration' | 'timeout' | 'transport' | 'unauthorized' | 'forbidden' | 'not_found' | 'conflict' | 'rate_limited' | 'server';
export class ClientError extends Error {
  constructor(readonly kind: ClientErrorKind, message: string, readonly status?: number, options?: ErrorOptions) { super(message, options); }
}

export const operations = {
  "listGatewayEndpointGroups": { id: "listGatewayEndpointGroups", method: "GET", path: "/api/admin/endpoint-groups", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayAdminHealth": { id: "getGatewayAdminHealth", method: "GET", path: "/api/admin/health", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "listGatewayOrchestratorRoutes": { id: "listGatewayOrchestratorRoutes", method: "GET", path: "/api/admin/orchestrator/routes", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "reloadGatewayOrchestratorRoutes": { id: "reloadGatewayOrchestratorRoutes", method: "POST", path: "/api/admin/orchestrator/routes/reload", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: ["application/json"], requestBodyRequired: false } as const,
  "getGatewayOrchestratorSnapshot": { id: "getGatewayOrchestratorSnapshot", method: "GET", path: "/api/admin/orchestrator/snapshot", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "listGatewayServices": { id: "listGatewayServices", method: "GET", path: "/api/admin/services", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "listGatewayServiceStatuses": { id: "listGatewayServiceStatuses", method: "GET", path: "/api/admin/services/status", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "listGatewayServiceOperations": { id: "listGatewayServiceOperations", method: "GET", path: "/api/admin/services/status/operations", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayServiceOperation": { id: "getGatewayServiceOperation", method: "GET", path: "/api/admin/services/status/operations/{id}", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayServiceStatus": { id: "getGatewayServiceStatus", method: "GET", path: "/api/admin/services/status/{id}", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayService": { id: "getGatewayService", method: "GET", path: "/api/admin/services/{id}", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayServiceTopology": { id: "getGatewayServiceTopology", method: "GET", path: "/api/admin/topology", audience: "internal", permission: "system.admin", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayUserContributionSnapshot": { id: "getGatewayUserContributionSnapshot", method: "GET", path: "/api/v1/contributions/snapshot", audience: "internal", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "deleteGatewayTopologyProjection": { id: "deleteGatewayTopologyProjection", method: "DELETE", path: "/api/v1/topologies/{id}", audience: "internal", headerParameters: [], requestContentTypes: ["application/json"], requestBodyRequired: true } as const,
  "getGatewayTopologyProjection": { id: "getGatewayTopologyProjection", method: "GET", path: "/api/v1/topologies/{id}", audience: "internal", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "applyGatewayTopologyProjection": { id: "applyGatewayTopologyProjection", method: "PUT", path: "/api/v1/topologies/{id}", audience: "internal", headerParameters: [], requestContentTypes: ["application/json"], requestBodyRequired: true } as const,
  "getGatewayCompatibilityHealth": { id: "getGatewayCompatibilityHealth", method: "GET", path: "/health", audience: "internal", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayLiveness": { id: "getGatewayLiveness", method: "GET", path: "/healthz", audience: "internal", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayMetrics": { id: "getGatewayMetrics", method: "GET", path: "/metrics", audience: "internal", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
  "getGatewayReadiness": { id: "getGatewayReadiness", method: "GET", path: "/readyz", audience: "internal", headerParameters: [], requestContentTypes: [], requestBodyRequired: false } as const,
} satisfies Readonly<Record<string, Operation>>;

function expandPath(template: string, parameters: Readonly<Record<string, string>>): string {
  return template.replace(/\{([^}]+)\}/g, (_match: string, key: string) => {
    const value = parameters[key]; if (value === undefined) throw new ClientError('configuration', `missing path parameter ${key}`); return encodeURIComponent(value);
  });
}

function errorKind(status: number): ClientErrorKind | undefined {
  if (status === 401) return 'unauthorized'; if (status === 403) return 'forbidden'; if (status === 404) return 'not_found';
  if (status === 409) return 'conflict'; if (status === 429) return 'rate_limited'; if (status >= 500) return 'server'; return undefined;
}

const forbiddenHeaders = new Set(['authorization', 'host', 'cookie', 'set-cookie', 'connection', 'keep-alive', 'te', 'trailer', 'transfer-encoding', 'upgrade', 'content-length', 'content-type', 'idempotency-key', 'forwarded', 'via', 'expect', 'x-api-key', 'api-key']);
const forbiddenHeaderPrefixes = ['proxy-', 'sec-', 'x-forwarded-', 'x-ojos-caller-', 'x-ojos-gateway-', 'x-ojos-internal-', 'x-ojos-workload-'];
function forbiddenHeader(name: string): boolean { const normalized = name.trim().toLowerCase(); return forbiddenHeaders.has(normalized) || forbiddenHeaderPrefixes.some((prefix) => normalized.startsWith(prefix)); }
function validHeaderName(name: string): boolean { return /^[!#$%&'*+\-.^_`|~0-9A-Za-z]+$/.test(name); }
function validHeaderValue(value: string): boolean { return !/[\x00-\x08\x0A-\x1F\x7F]/.test(value); }
function selectContentType(operation: Operation, requested: string | undefined, hasBody: boolean): string | undefined {
  const normalized = requested?.trim();
  if (!hasBody) { if (normalized) throw new ClientError('configuration', 'content type cannot be set without a request body'); return undefined; }
  if (!normalized) return operation.requestContentTypes[0];
  const declared = operation.requestContentTypes.find((value) => value.toLowerCase() === normalized.toLowerCase());
  if (!declared) throw new ClientError('configuration', `content type ${normalized} is not declared for operation ${operation.id}`);
  return declared;
}
function jsonMediaType(contentType: string | undefined): boolean {
  const mediaType = contentType?.split(';', 1)[0]?.trim().toLowerCase();
  return mediaType === 'application/json' || mediaType?.endsWith('+json') === true;
}

export class Client {
  readonly #context: ContextSnapshotProvider; readonly #fetch: FetchLike; readonly #timeoutMs: number;
  constructor(options: ClientOptions) { this.#context = options.context; this.#fetch = options.fetch ?? globalThis.fetch.bind(globalThis); this.#timeoutMs = options.timeoutMs ?? 30_000; }

  async call(operation: Operation, options: CallOptions = {}): Promise<Response> {
    const snapshot = await this.#context();
    if (!snapshot.baseUrl.trim()) throw new ClientError('configuration', 'context snapshot base URL is required');
    const target = new URL(snapshot.baseUrl.replace(/\/$/, '') + expandPath(operation.path, options.path ?? {}));
    for (const [key, value] of Object.entries(options.query ?? {})) if (value !== undefined) target.searchParams.set(key, String(value));
    if (options.body !== undefined && options.rawBody !== undefined) throw new ClientError('configuration', 'body and raw body cannot both be set');
    const hasBody = options.body !== undefined || options.rawBody !== undefined;
    if (operation.requestBodyRequired && !hasBody) throw new ClientError('configuration', 'request body is required');
    const contentType = selectContentType(operation, options.contentType, hasBody);
    const headers = new Headers();
    for (const [name, value] of Object.entries(options.headers ?? {})) {
      if (!validHeaderName(name)) throw new ClientError('configuration', `invalid header name ${JSON.stringify(name)}`);
      if (forbiddenHeader(name)) throw new ClientError('configuration', `header ${name} cannot be overridden`);
      if (!validHeaderValue(value)) throw new ClientError('configuration', `invalid value for header ${name}`);
      headers.append(name, value);
    }
    if (contentType) headers.set('Content-Type', contentType); if (snapshot.token) headers.set('Authorization', `Bearer ${snapshot.token}`);
    if (options.idempotencyKey?.trim()) headers.set('Idempotency-Key', options.idempotencyKey);
    for (const parameter of operation.headerParameters) if (parameter.required && !headers.get(parameter.name)?.trim()) throw new ClientError('configuration', `required header ${parameter.name} is missing`);
    const controller = new AbortController(); const timeout = setTimeout(() => controller.abort(new DOMException('request timed out', 'TimeoutError')), options.timeoutMs ?? this.#timeoutMs);
    const abort = () => controller.abort(options.signal?.reason); options.signal?.addEventListener('abort', abort, { once: true });
    try {
      const requestBody = options.rawBody !== undefined ? options.rawBody : options.body === undefined ? undefined : jsonMediaType(contentType) ? JSON.stringify(options.body) : options.body as BodyInit;
      const response = await this.#fetch(target, { method: operation.method, headers, body: requestBody, signal: controller.signal });
      const kind = errorKind(response.status); if (kind) throw new ClientError(kind, `HTTP ${response.status}`, response.status); return response;
    } catch (error) {
      if (error instanceof ClientError) throw error;
      if (controller.signal.aborted && !options.signal?.aborted) throw new ClientError('timeout', 'request timed out', undefined, { cause: error });
      throw new ClientError('transport', 'request failed', undefined, { cause: error });
    } finally { clearTimeout(timeout); options.signal?.removeEventListener('abort', abort); }
  }

  listgatewayendpointgroups(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["listGatewayEndpointGroups"], options);
  }

  getgatewayadminhealth(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayAdminHealth"], options);
  }

  listgatewayorchestratorroutes(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["listGatewayOrchestratorRoutes"], options);
  }

  reloadgatewayorchestratorroutes(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["reloadGatewayOrchestratorRoutes"], options);
  }

  getgatewayorchestratorsnapshot(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayOrchestratorSnapshot"], options);
  }

  listgatewayservices(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["listGatewayServices"], options);
  }

  listgatewayservicestatuses(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["listGatewayServiceStatuses"], options);
  }

  listgatewayserviceoperations(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["listGatewayServiceOperations"], options);
  }

  getgatewayserviceoperation(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayServiceOperation"], options);
  }

  getgatewayservicestatus(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayServiceStatus"], options);
  }

  getgatewayservice(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayService"], options);
  }

  getgatewayservicetopology(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayServiceTopology"], options);
  }

  getgatewayusercontributionsnapshot(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayUserContributionSnapshot"], options);
  }

  deletegatewaytopologyprojection(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["deleteGatewayTopologyProjection"], options);
  }

  getgatewaytopologyprojection(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayTopologyProjection"], options);
  }

  applygatewaytopologyprojection(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["applyGatewayTopologyProjection"], options);
  }

  getgatewaycompatibilityhealth(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayCompatibilityHealth"], options);
  }

  getgatewayliveness(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayLiveness"], options);
  }

  getgatewaymetrics(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayMetrics"], options);
  }

  getgatewayreadiness(options: CallOptions = {}): Promise<Response> {
    return this.call(operations["getGatewayReadiness"], options);
  }
}
